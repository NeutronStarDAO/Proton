use candid::{CandidType, Deserialize, Principal, Encode, Decode};
use serde_bytes;
use crate::utils::build_local_agent;
use crate::USERA_PEM;

// pub async fn create_post(content: String) -> String {
//     let response_blob = build_local_agent(USERA_PEM)
//         .update(
            
//         )
// }



